---
- hosts: local
  tasks:
      - name: APT package management tasks
        block:
            - include_role:
                  name: APT
        tags: apt

      - name: WSL2 configuration tasks
        block:
            - include_role:
                  name: WSL
        tags: wsl

      # - include_role:
      #       name: SYMLINKS

      # - include_role:
      #       name: BOOTSTRAPPING

      # - include_role:
      #       name: HOMEBREW

      # - include_role:
      #       name: DOTFILES

      # - include_role:
      #       name: SSH

      # - include_role:
      #       name: PYTHON

      # - include_role:
      #       name: DOCKER

      # - include_role:
      #       name: SPORTINGSOLUTION

      # - block:
      #       - include_role:
      #             name: AWS
      #   tags: aws
